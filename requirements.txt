fastapi==0.100.0       # Framework principal para crear la API
uvicorn==0.22.0        # Servidor ASGI que ejecuta la aplicación FastAPI
psycopg2-binary==2.9.7 # Cliente para conectarse a PostgreSQL
python-dotenv==1.0.0   # Para cargar variables de entorno desde el archivo .env
